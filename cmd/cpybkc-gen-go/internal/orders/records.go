// Code generated by cpybkc-gen-go. DO NOT EDIT.

package orders

import "math/big"

// OrderRecord is the ORDER-RECORD record, as docs/ir/SPEC.md resolved it.
type OrderRecord struct {
	// OrderId is ORDER-ID — numeric, DISPLAY, 5 digits, unsigned, 5 bytes.
	OrderId int32

	// CustomerName is CUSTOMER-NAME — alphanumeric, DISPLAY, 20 bytes.
	CustomerName string

	// OrderTotal is ORDER-TOTAL — numeric, PACKED-DECIMAL, 7 digits, signed, 4 bytes.
	// The value is unscaled: the item's value is this field times 10^-2.
	OrderTotal int32

	// LineItem is LINE-ITEM — a group of 3 members, OCCURS 3.
	LineItem [3]struct {
		// Sku is SKU — alphanumeric, DISPLAY, 8 bytes.
		Sku string

		// Quantity is QUANTITY — numeric, BINARY, 4 digits, signed, 2 bytes.
		Quantity int16

		// slack is the bytes retained for the slack nodes among this item's
		// members, in the order those nodes occupy the record: one run each, as
		// they stood when the record was read, and one set of them per occurrence
		// of this struct. A nil run is one the record does not carry; an empty run
		// is a run of no bytes, and the two are not the same.
		//
		// They travel with the record and there is nothing here for a caller to do.
		// See docs/ir/SPEC.md, "Slack survives a read".
		slack [1][]byte
	}

	// DetailCount is DETAIL-COUNT — numeric, DISPLAY, 3 digits, unsigned, 3 bytes.
	DetailCount int32

	// Detail is DETAIL — a group of 1 member, OCCURS 0 TO 12 DEPENDING ON DETAIL-COUNT.
	Detail []struct {
		// DetailText is DETAIL-TEXT — alphanumeric, DISPLAY, 10 bytes.
		DetailText string
	}

	// slack is the bytes retained for the slack nodes among this item's
	// members, in the order those nodes occupy the record: one run each, as
	// they stood when the record was read, and one set of them per occurrence
	// of this struct. A nil run is one the record does not carry; an empty run
	// is a run of no bytes, and the two are not the same.
	//
	// They travel with the record and there is nothing here for a caller to do.
	// See docs/ir/SPEC.md, "Slack survives a read".
	slack [1][]byte
}

// TrailerRecord is the TRAILER-RECORD record, as docs/ir/SPEC.md resolved it.
type TrailerRecord struct {
	// GrandTotal is GRAND-TOTAL — numeric, PACKED-DECIMAL, 20 digits, signed, 11 bytes.
	GrandTotal *big.Int

	// ExchangeRate is EXCHANGE-RATE — COMP-1, 4 bytes.
	ExchangeRate float32

	// TableIndex is TABLE-INDEX — INDEX, 4 bytes.
	TableIndex []byte

	// PrintedTotal is PRINTED-TOTAL — numeric-edited, DISPLAY, 12 bytes, OCCURS 2.
	PrintedTotal [2]string
}
