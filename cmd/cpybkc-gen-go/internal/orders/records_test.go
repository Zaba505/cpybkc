// Code generated by cpybkc-gen-go. DO NOT EDIT.

// The record tier of this package's generated tests: one case per record the
// layout describes, and one per arm of an alternation inside one.
//
// Each case carries the bytes it reads as a literal, decodes them, checks every
// field against the value the literal was laid out with, and writes the record
// back out byte for byte. The bytes are synthesized from the layout rather than
// read from a file, so they are what to hold against the file on your desk: the
// comment column names the item, the offset summed from the widths ahead of it,
// and the picture.
//
// Nothing here is yours to edit — this directory is regenerated whole. Put your
// own tests in a package of your own that imports orders.
package orders_test

import (
	"bytes"
	"github.com/Zaba505/cobol-go/codec"
	"github.com/Zaba505/cpybkc/cmd/cpybkc-gen-go/internal/orders"
	"math/big"
	"testing"
)

// TestOrderRecordReadsBackTheBytesItWasReadFrom is ORDER-RECORD: the bytes
// below, every field they decode into, and the same bytes written back.
func TestOrderRecordReadsBackTheBytesItWasReadFrom(t *testing.T) {
	t.Parallel()

	in := []byte{
		0xf0, 0xf0, 0xf0, 0xf0, 0xf1, // ORDER-ID @0 9(5)
		0xc6, 0xc6, 0xc6, 0xc6, 0xc6, 0xc6, 0xc6, 0xc6, // CUSTOMER-NAME @5 X(20)
		0xc6, 0xc6, 0xc6, 0xc6, 0xc6, 0xc6, 0xc6, 0xc6, //
		0xc6, 0xc6, 0xc6, 0xc6, //
		0xb9, 0xba, // (slack) @25 2 bytes no item covers
		0x00, 0x00, 0x02, 0x8d, // ORDER-TOTAL @27 S9(5)V9(2) PACKED-DECIMAL
		0xc6, 0xc6, 0xc6, 0xc6, 0xc6, 0xc6, 0xc6, 0xc6, // LINE-ITEM(1).SKU @31 X(8)
		0xff, 0xd8, // LINE-ITEM(1).QUANTITY @39 S9(4) BINARY
		0xc9,                                           // LINE-ITEM(1).(slack) @41 1 byte no item covers
		0xca,                                           // LINE-ITEM(1).FILLER @42 X(1)
		0xd9, 0xd9, 0xd9, 0xd9, 0xd9, 0xd9, 0xd9, 0xd9, // LINE-ITEM(2).SKU @43 X(8)
		0xff, 0xcc, // LINE-ITEM(2).QUANTITY @51 S9(4) BINARY
		0xd5,                                           // LINE-ITEM(2).(slack) @53 1 byte no item covers
		0xd6,                                           // LINE-ITEM(2).FILLER @54 X(1)
		0xc4, 0xc4, 0xc4, 0xc4, 0xc4, 0xc4, 0xc4, 0xc4, // LINE-ITEM(3).SKU @55 X(8)
		0xff, 0xc0, // LINE-ITEM(3).QUANTITY @63 S9(4) BINARY
		0xe1,                   // LINE-ITEM(3).(slack) @65 1 byte no item covers
		0xe2,                   // LINE-ITEM(3).FILLER @66 X(1)
		0xe3, 0xe4, 0xe5, 0xe6, // FILLER @67 X(4)
		0xe3, 0xe3, // NOTE-CODE @71 X(2)
		0xf0, 0xf0, 0xf1, // DETAIL-COUNT @73 9(3)
		0xe8, 0xe8, 0xe8, 0xe8, 0xe8, 0xe8, 0xe8, 0xe8, // DETAIL(1).DETAIL-TEXT @76 X(10)
		0xe8, 0xe8, //
	}

	r, err := codec.NewReader(bytes.NewReader(in), orders.Encoding())
	if err != nil {
		t.Fatalf("codec.NewReader: %v", err)
	}

	var record orders.OrderRecord

	if err := record.UnmarshalCOBOL(r); err != nil {
		t.Fatalf("UnmarshalCOBOL: %v", err)
	}

	if record.OrderID != 1 {
		t.Errorf("ORDER-ID: got %d, want %d", record.OrderID, 1)
	}

	if record.CustomerName != "FFFFFFFFFFFFFFFFFFFF" {
		t.Errorf("CUSTOMER-NAME: got %q, want %q", record.CustomerName, "FFFFFFFFFFFFFFFFFFFF")
	}

	if record.OrderTotal != -28 {
		t.Errorf("ORDER-TOTAL: got %d, want %d", record.OrderTotal, -28)
	}

	if record.LineItem[0].Sku != "FFFFFFFF" {
		t.Errorf("LINE-ITEM(1).SKU: got %q, want %q", record.LineItem[0].Sku, "FFFFFFFF")
	}

	if record.LineItem[0].Quantity != -40 {
		t.Errorf("LINE-ITEM(1).QUANTITY: got %d, want %d", record.LineItem[0].Quantity, -40)
	}

	if record.LineItem[1].Sku != "RRRRRRRR" {
		t.Errorf("LINE-ITEM(2).SKU: got %q, want %q", record.LineItem[1].Sku, "RRRRRRRR")
	}

	if record.LineItem[1].Quantity != -52 {
		t.Errorf("LINE-ITEM(2).QUANTITY: got %d, want %d", record.LineItem[1].Quantity, -52)
	}

	if record.LineItem[2].Sku != "DDDDDDDD" {
		t.Errorf("LINE-ITEM(3).SKU: got %q, want %q", record.LineItem[2].Sku, "DDDDDDDD")
	}

	if record.LineItem[2].Quantity != -64 {
		t.Errorf("LINE-ITEM(3).QUANTITY: got %d, want %d", record.LineItem[2].Quantity, -64)
	}

	if record.NoteCode != "TT" {
		t.Errorf("NOTE-CODE: got %q, want %q", record.NoteCode, "TT")
	}

	if record.DetailCount != 1 {
		t.Errorf("DETAIL-COUNT: got %d, want %d", record.DetailCount, 1)
	}

	if len(record.Detail) != 1 {
		t.Fatalf("DETAIL: got %d occurrences, want %d", len(record.Detail), 1)
	}

	if record.Detail[0].DetailText != "YYYYYYYYYY" {
		t.Errorf("DETAIL(1).DETAIL-TEXT: got %q, want %q", record.Detail[0].DetailText, "YYYYYYYYYY")
	}

	var out bytes.Buffer

	w, err := codec.NewWriter(&out, orders.Encoding())
	if err != nil {
		t.Fatalf("codec.NewWriter: %v", err)
	}

	if err := record.MarshalCOBOL(w); err != nil {
		t.Fatalf("MarshalCOBOL: %v", err)
	}

	if !bytes.Equal(out.Bytes(), in) {
		t.Errorf("the record does not write back the bytes it was read from\n got: % x\nwant: % x", out.Bytes(), in)
	}
}

// TestTrailerRecordReadsBackTheBytesItWasReadFrom is TRAILER-RECORD: the bytes
// below, every field they decode into, and the same bytes written back.
func TestTrailerRecordReadsBackTheBytesItWasReadFrom(t *testing.T) {
	t.Parallel()

	in := []byte{
		0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, // GRAND-TOTAL @0 S9(20) PACKED-DECIMAL
		0x00, 0x00, 0x1d, //
		0x41, 0xc8, 0x00, 0x00, // EXCHANGE-RATE @11 COMP-1
		0xaf, 0xb0, 0xb1, 0xb2, // TABLE-INDEX @15 INDEX
		0xe3, 0xe3, 0xe3, 0xe3, 0xe3, 0xe3, 0xe3, 0xe3, // PRINTED-TOTAL(1) @19 numeric-edited, 12 bytes
		0xe3, 0xe3, 0xe3, 0xe3, //
		0xc6, 0xc6, 0xc6, 0xc6, 0xc6, 0xc6, 0xc6, 0xc6, // PRINTED-TOTAL(2) @31 numeric-edited, 12 bytes
		0xc6, 0xc6, 0xc6, 0xc6, //
	}

	r, err := codec.NewReader(bytes.NewReader(in), orders.Encoding())
	if err != nil {
		t.Fatalf("codec.NewReader: %v", err)
	}

	var record orders.TrailerRecord

	if err := record.UnmarshalCOBOL(r); err != nil {
		t.Fatalf("UnmarshalCOBOL: %v", err)
	}

	if want, _ := new(big.Int).SetString("-1", 10); record.GrandTotal == nil || record.GrandTotal.Cmp(want) != 0 {
		t.Errorf("GRAND-TOTAL: got %v, want %v", record.GrandTotal, want)
	}

	if record.ExchangeRate != 12.5 {
		t.Errorf("EXCHANGE-RATE: got %v, want %v", record.ExchangeRate, 12.5)
	}

	if want := []byte{0xaf, 0xb0, 0xb1, 0xb2}; !bytes.Equal(record.TableIndex, want) {
		t.Errorf("TABLE-INDEX: got % x, want % x", record.TableIndex, want)
	}

	if record.PrintedTotal[0] != "TTTTTTTTTTTT" {
		t.Errorf("PRINTED-TOTAL(1): got %q, want %q", record.PrintedTotal[0], "TTTTTTTTTTTT")
	}

	if record.PrintedTotal[1] != "FFFFFFFFFFFF" {
		t.Errorf("PRINTED-TOTAL(2): got %q, want %q", record.PrintedTotal[1], "FFFFFFFFFFFF")
	}

	var out bytes.Buffer

	w, err := codec.NewWriter(&out, orders.Encoding())
	if err != nil {
		t.Fatalf("codec.NewWriter: %v", err)
	}

	if err := record.MarshalCOBOL(w); err != nil {
		t.Fatalf("MarshalCOBOL: %v", err)
	}

	if !bytes.Equal(out.Bytes(), in) {
		t.Errorf("the record does not write back the bytes it was read from\n got: % x\nwant: % x", out.Bytes(), in)
	}
}

// TestSyncRecordReadsBackTheBytesItWasReadFrom is SYNC-RECORD: the bytes
// below, every field they decode into, and the same bytes written back.
func TestSyncRecordReadsBackTheBytesItWasReadFrom(t *testing.T) {
	t.Parallel()

	in := []byte{
		0xe8,             // SYNC-FLAG @0 X(1)
		0xa1, 0xa2, 0xa3, // (slack) @1 3 bytes no item covers
		0xff, 0xff, 0xff, 0xfb, // SYNC-COUNTER @4 S9(9) BINARY
		0xa8, 0xa9, 0xaa, 0xab, 0xac, 0xad, 0xae, 0xaf, // (slack) @8 8 bytes no item covers
	}

	r, err := codec.NewReader(bytes.NewReader(in), orders.Encoding())
	if err != nil {
		t.Fatalf("codec.NewReader: %v", err)
	}

	var record orders.SyncRecord

	if err := record.UnmarshalCOBOL(r); err != nil {
		t.Fatalf("UnmarshalCOBOL: %v", err)
	}

	if record.SyncFlag != "Y" {
		t.Errorf("SYNC-FLAG: got %q, want %q", record.SyncFlag, "Y")
	}

	if record.SyncCounter != -5 {
		t.Errorf("SYNC-COUNTER: got %d, want %d", record.SyncCounter, -5)
	}

	var out bytes.Buffer

	w, err := codec.NewWriter(&out, orders.Encoding())
	if err != nil {
		t.Fatalf("codec.NewWriter: %v", err)
	}

	if err := record.MarshalCOBOL(w); err != nil {
		t.Fatalf("MarshalCOBOL: %v", err)
	}

	if !bytes.Equal(out.Bytes(), in) {
		t.Errorf("the record does not write back the bytes it was read from\n got: % x\nwant: % x", out.Bytes(), in)
	}
}

// TestTableRecordReadsBackTheBytesItWasReadFrom is TABLE-RECORD: the bytes
// below, every field they decode into, and the same bytes written back.
func TestTableRecordReadsBackTheBytesItWasReadFrom(t *testing.T) {
	t.Parallel()

	in := []byte{
		0xf0, 0xf1, // PAIR-COUNT @0 9(2)
		0xc3, 0xc3, 0xc3, // LEFT-ITEM(1).LEFT-TEXT @2 X(3)
		0xc6, 0xc6, // RIGHT-ITEM(1).RIGHT-TEXT @5 X(2)
		0xc8, // BLOCK(1).BLOCK-ITEM(1).BLOCK-TEXT @7 X(1)
	}

	r, err := codec.NewReader(bytes.NewReader(in), orders.Encoding())
	if err != nil {
		t.Fatalf("codec.NewReader: %v", err)
	}

	var record orders.TableRecord

	if err := record.UnmarshalCOBOL(r); err != nil {
		t.Fatalf("UnmarshalCOBOL: %v", err)
	}

	if record.PairCount != 1 {
		t.Errorf("PAIR-COUNT: got %d, want %d", record.PairCount, 1)
	}

	if len(record.LeftItem) != 1 {
		t.Fatalf("LEFT-ITEM: got %d occurrences, want %d", len(record.LeftItem), 1)
	}

	if record.LeftItem[0].LeftText != "CCC" {
		t.Errorf("LEFT-ITEM(1).LEFT-TEXT: got %q, want %q", record.LeftItem[0].LeftText, "CCC")
	}

	if len(record.RightItem) != 1 {
		t.Fatalf("RIGHT-ITEM: got %d occurrences, want %d", len(record.RightItem), 1)
	}

	if record.RightItem[0].RightText != "FF" {
		t.Errorf("RIGHT-ITEM(1).RIGHT-TEXT: got %q, want %q", record.RightItem[0].RightText, "FF")
	}

	if len(record.Block) != 1 {
		t.Fatalf("BLOCK: got %d occurrences, want %d", len(record.Block), 1)
	}

	if len(record.Block[0].BlockItem) != 1 {
		t.Fatalf("BLOCK(1).BLOCK-ITEM: got %d occurrences, want %d", len(record.Block[0].BlockItem), 1)
	}

	if record.Block[0].BlockItem[0].BlockText != "H" {
		t.Errorf("BLOCK(1).BLOCK-ITEM(1).BLOCK-TEXT: got %q, want %q", record.Block[0].BlockItem[0].BlockText, "H")
	}

	var out bytes.Buffer

	w, err := codec.NewWriter(&out, orders.Encoding())
	if err != nil {
		t.Fatalf("codec.NewWriter: %v", err)
	}

	if err := record.MarshalCOBOL(w); err != nil {
		t.Fatalf("MarshalCOBOL: %v", err)
	}

	if !bytes.Equal(out.Bytes(), in) {
		t.Errorf("the record does not write back the bytes it was read from\n got: % x\nwant: % x", out.Bytes(), in)
	}
}

// TestEntryRecordReadsBackTheBytesItWasReadFrom is ENTRY-RECORD: the bytes
// below, every field they decode into, and the same bytes written back.
func TestEntryRecordReadsBackTheBytesItWasReadFrom(t *testing.T) {
	t.Parallel()

	in := []byte{
		0xc4,                   // ENTRY(1).ENTRY-TYPE @0 X(1)
		0xc2, 0xc2, 0xc2, 0xc2, // ENTRY(1).ENTRY-DETAIL.DETAIL-SKU @1 X(4)
		0xff, 0xfa, // ENTRY(1).ENTRY-DETAIL.DETAIL-QTY @5 S9(4) BINARY
		0xc4,                   // ENTRY(2).ENTRY-TYPE @7 X(1)
		0xc9, 0xc9, 0xc9, 0xc9, // ENTRY(2).ENTRY-DETAIL.DETAIL-SKU @8 X(4)
		0xff, 0xf3, // ENTRY(2).ENTRY-DETAIL.DETAIL-QTY @12 S9(4) BINARY
		0xc4,                   // ENTRY(3).ENTRY-TYPE @14 X(1)
		0xd7, 0xd7, 0xd7, 0xd7, // ENTRY(3).ENTRY-DETAIL.DETAIL-SKU @15 X(4)
		0xff, 0xec, // ENTRY(3).ENTRY-DETAIL.DETAIL-QTY @19 S9(4) BINARY
	}

	r, err := codec.NewReader(bytes.NewReader(in), orders.Encoding())
	if err != nil {
		t.Fatalf("codec.NewReader: %v", err)
	}

	var record orders.EntryRecord

	if err := record.UnmarshalCOBOL(r); err != nil {
		t.Fatalf("UnmarshalCOBOL: %v", err)
	}

	if record.Entry[0].EntryType != "D" {
		t.Errorf("ENTRY(1).ENTRY-TYPE: got %q, want %q", record.Entry[0].EntryType, "D")
	}

	if record.Entry[0].EntryDetail == nil {
		t.Fatalf("ENTRY(1).ENTRY-DETAIL: the record holds no arm, and its bytes select this one")
	}

	if record.Entry[0].EntrySummary != nil {
		t.Errorf("ENTRY(1).ENTRY-SUMMARY: the record holds this arm, and its bytes select another")
	}

	if record.Entry[0].EntryDetail.DetailSku != "BBBB" {
		t.Errorf("ENTRY(1).ENTRY-DETAIL.DETAIL-SKU: got %q, want %q", record.Entry[0].EntryDetail.DetailSku, "BBBB")
	}

	if record.Entry[0].EntryDetail.DetailQty != -6 {
		t.Errorf("ENTRY(1).ENTRY-DETAIL.DETAIL-QTY: got %d, want %d", record.Entry[0].EntryDetail.DetailQty, -6)
	}

	if record.Entry[1].EntryType != "D" {
		t.Errorf("ENTRY(2).ENTRY-TYPE: got %q, want %q", record.Entry[1].EntryType, "D")
	}

	if record.Entry[1].EntryDetail == nil {
		t.Fatalf("ENTRY(2).ENTRY-DETAIL: the record holds no arm, and its bytes select this one")
	}

	if record.Entry[1].EntrySummary != nil {
		t.Errorf("ENTRY(2).ENTRY-SUMMARY: the record holds this arm, and its bytes select another")
	}

	if record.Entry[1].EntryDetail.DetailSku != "IIII" {
		t.Errorf("ENTRY(2).ENTRY-DETAIL.DETAIL-SKU: got %q, want %q", record.Entry[1].EntryDetail.DetailSku, "IIII")
	}

	if record.Entry[1].EntryDetail.DetailQty != -13 {
		t.Errorf("ENTRY(2).ENTRY-DETAIL.DETAIL-QTY: got %d, want %d", record.Entry[1].EntryDetail.DetailQty, -13)
	}

	if record.Entry[2].EntryType != "D" {
		t.Errorf("ENTRY(3).ENTRY-TYPE: got %q, want %q", record.Entry[2].EntryType, "D")
	}

	if record.Entry[2].EntryDetail == nil {
		t.Fatalf("ENTRY(3).ENTRY-DETAIL: the record holds no arm, and its bytes select this one")
	}

	if record.Entry[2].EntrySummary != nil {
		t.Errorf("ENTRY(3).ENTRY-SUMMARY: the record holds this arm, and its bytes select another")
	}

	if record.Entry[2].EntryDetail.DetailSku != "PPPP" {
		t.Errorf("ENTRY(3).ENTRY-DETAIL.DETAIL-SKU: got %q, want %q", record.Entry[2].EntryDetail.DetailSku, "PPPP")
	}

	if record.Entry[2].EntryDetail.DetailQty != -20 {
		t.Errorf("ENTRY(3).ENTRY-DETAIL.DETAIL-QTY: got %d, want %d", record.Entry[2].EntryDetail.DetailQty, -20)
	}

	var out bytes.Buffer

	w, err := codec.NewWriter(&out, orders.Encoding())
	if err != nil {
		t.Fatalf("codec.NewWriter: %v", err)
	}

	if err := record.MarshalCOBOL(w); err != nil {
		t.Fatalf("MarshalCOBOL: %v", err)
	}

	if !bytes.Equal(out.Bytes(), in) {
		t.Errorf("the record does not write back the bytes it was read from\n got: % x\nwant: % x", out.Bytes(), in)
	}
}

// TestEntryRecordHoldingEntrySummaryReadsBackTheBytesItWasReadFrom is
// ENTRY-RECORD: the bytes below, every field they decode into, and the same
// bytes written back.
//
// The alternation this case is for holds ENTRY-SUMMARY.
func TestEntryRecordHoldingEntrySummaryReadsBackTheBytesItWasReadFrom(t *testing.T) {
	t.Parallel()

	in := []byte{
		0xe2,                   // ENTRY(1).ENTRY-TYPE @0 X(1)
		0xc2, 0xc2, 0xc2, 0xc2, // ENTRY(1).ENTRY-SUMMARY.SUMMARY-TEXT @1 X(4)
		0xa5, 0xa6, // ENTRY(1).ENTRY-SUMMARY.(slack) @5 2 bytes no item covers
		0xe2,                   // ENTRY(2).ENTRY-TYPE @7 X(1)
		0xc9, 0xc9, 0xc9, 0xc9, // ENTRY(2).ENTRY-SUMMARY.SUMMARY-TEXT @8 X(4)
		0xac, 0xad, // ENTRY(2).ENTRY-SUMMARY.(slack) @12 2 bytes no item covers
		0xe2,                   // ENTRY(3).ENTRY-TYPE @14 X(1)
		0xd7, 0xd7, 0xd7, 0xd7, // ENTRY(3).ENTRY-SUMMARY.SUMMARY-TEXT @15 X(4)
		0xb3, 0xb4, // ENTRY(3).ENTRY-SUMMARY.(slack) @19 2 bytes no item covers
	}

	r, err := codec.NewReader(bytes.NewReader(in), orders.Encoding())
	if err != nil {
		t.Fatalf("codec.NewReader: %v", err)
	}

	var record orders.EntryRecord

	if err := record.UnmarshalCOBOL(r); err != nil {
		t.Fatalf("UnmarshalCOBOL: %v", err)
	}

	if record.Entry[0].EntryType != "S" {
		t.Errorf("ENTRY(1).ENTRY-TYPE: got %q, want %q", record.Entry[0].EntryType, "S")
	}

	if record.Entry[0].EntryDetail != nil {
		t.Errorf("ENTRY(1).ENTRY-DETAIL: the record holds this arm, and its bytes select another")
	}

	if record.Entry[0].EntrySummary == nil {
		t.Fatalf("ENTRY(1).ENTRY-SUMMARY: the record holds no arm, and its bytes select this one")
	}

	if record.Entry[0].EntrySummary.SummaryText != "BBBB" {
		t.Errorf("ENTRY(1).ENTRY-SUMMARY.SUMMARY-TEXT: got %q, want %q", record.Entry[0].EntrySummary.SummaryText, "BBBB")
	}

	if record.Entry[1].EntryType != "S" {
		t.Errorf("ENTRY(2).ENTRY-TYPE: got %q, want %q", record.Entry[1].EntryType, "S")
	}

	if record.Entry[1].EntryDetail != nil {
		t.Errorf("ENTRY(2).ENTRY-DETAIL: the record holds this arm, and its bytes select another")
	}

	if record.Entry[1].EntrySummary == nil {
		t.Fatalf("ENTRY(2).ENTRY-SUMMARY: the record holds no arm, and its bytes select this one")
	}

	if record.Entry[1].EntrySummary.SummaryText != "IIII" {
		t.Errorf("ENTRY(2).ENTRY-SUMMARY.SUMMARY-TEXT: got %q, want %q", record.Entry[1].EntrySummary.SummaryText, "IIII")
	}

	if record.Entry[2].EntryType != "S" {
		t.Errorf("ENTRY(3).ENTRY-TYPE: got %q, want %q", record.Entry[2].EntryType, "S")
	}

	if record.Entry[2].EntryDetail != nil {
		t.Errorf("ENTRY(3).ENTRY-DETAIL: the record holds this arm, and its bytes select another")
	}

	if record.Entry[2].EntrySummary == nil {
		t.Fatalf("ENTRY(3).ENTRY-SUMMARY: the record holds no arm, and its bytes select this one")
	}

	if record.Entry[2].EntrySummary.SummaryText != "PPPP" {
		t.Errorf("ENTRY(3).ENTRY-SUMMARY.SUMMARY-TEXT: got %q, want %q", record.Entry[2].EntrySummary.SummaryText, "PPPP")
	}

	var out bytes.Buffer

	w, err := codec.NewWriter(&out, orders.Encoding())
	if err != nil {
		t.Fatalf("codec.NewWriter: %v", err)
	}

	if err := record.MarshalCOBOL(w); err != nil {
		t.Fatalf("MarshalCOBOL: %v", err)
	}

	if !bytes.Equal(out.Bytes(), in) {
		t.Errorf("the record does not write back the bytes it was read from\n got: % x\nwant: % x", out.Bytes(), in)
	}
}

// TestShapeRecordReadsBackTheBytesItWasReadFrom is SHAPE-RECORD: the bytes
// below, every field they decode into, and the same bytes written back.
func TestShapeRecordReadsBackTheBytesItWasReadFrom(t *testing.T) {
	t.Parallel()

	in := []byte{
		0x00, 0x00, 0x01, // TALLY @0 9(5) COMP-6
		0x41, 0x48, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, // RATE @3 COMP-2
		0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xff, 0xf4, // WIDE-COUNT @11 S9(18) COMP-5
		0x00, 0x14, // UNSIGNED-COUNT @19 9(4) COMP-5
		0xb5, 0xb6, 0xb7, 0xb8, // ANCHOR @21 POINTER
		0xb9, 0xba, 0xbb, 0xbc, 0xbd, 0xbe, // WIDE-TEXT @25 NATIONAL
	}

	r, err := codec.NewReader(bytes.NewReader(in), orders.Encoding())
	if err != nil {
		t.Fatalf("codec.NewReader: %v", err)
	}

	var record orders.ShapeRecord

	if err := record.UnmarshalCOBOL(r); err != nil {
		t.Fatalf("UnmarshalCOBOL: %v", err)
	}

	if record.Tally != 1 {
		t.Errorf("TALLY: got %d, want %d", record.Tally, 1)
	}

	if record.Rate != 4.5 {
		t.Errorf("RATE: got %v, want %v", record.Rate, 4.5)
	}

	if record.WideCount != -12 {
		t.Errorf("WIDE-COUNT: got %d, want %d", record.WideCount, -12)
	}

	if record.UnsignedCount != 20 {
		t.Errorf("UNSIGNED-COUNT: got %d, want %d", record.UnsignedCount, 20)
	}

	if want := []byte{0xb5, 0xb6, 0xb7, 0xb8}; !bytes.Equal(record.Anchor, want) {
		t.Errorf("ANCHOR: got % x, want % x", record.Anchor, want)
	}

	if want := []byte{0xb9, 0xba, 0xbb, 0xbc, 0xbd, 0xbe}; !bytes.Equal(record.WideText, want) {
		t.Errorf("WIDE-TEXT: got % x, want % x", record.WideText, want)
	}

	var out bytes.Buffer

	w, err := codec.NewWriter(&out, orders.Encoding())
	if err != nil {
		t.Fatalf("codec.NewWriter: %v", err)
	}

	if err := record.MarshalCOBOL(w); err != nil {
		t.Fatalf("MarshalCOBOL: %v", err)
	}

	if !bytes.Equal(out.Bytes(), in) {
		t.Errorf("the record does not write back the bytes it was read from\n got: % x\nwant: % x", out.Bytes(), in)
	}
}
