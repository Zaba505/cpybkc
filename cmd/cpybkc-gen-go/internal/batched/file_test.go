// Code generated by cpybkc-gen-go. DO NOT EDIT.

// The file tier of this package's generated tests: one case per path through the
// automaton this layout describes, and between them every transition predicate it
// carries and every literal of a set-membership one.
//
// Each case carries a whole file as a literal — the framing as well as the records
// — reads it with the generated Reader, checks that the record types came back in
// the order the automaton admits them, writes them back with the generated Writer
// and checks that the bytes are the bytes it started from. The file is synthesized
// from the layout rather than read from a dataset, so it is what to hold against
// the file on your desk: the comment column names each run, the offset inside its
// record, and the picture.
//
// Nothing here is yours to edit — this directory is regenerated whole. Put your
// own tests in a package of your own that imports batched.
package batched_test

import (
	"bytes"
	"errors"
	"github.com/Zaba505/cpybkc/cmd/cpybkc-gen-go/internal/batched"
	"io"
	"testing"
)

// TestTwoBatchHeaders is a file of two BATCH-HEADER records: the bytes below,
// the records they read back as, and the same bytes written back.
//
// The discriminators it walks: HDR-TYPE holding "\xc8\xc4".
func TestTwoBatchHeaders(t *testing.T) {
	t.Parallel()

	in := []byte{
		0xc8, 0xc4, // record 1: HDR-TYPE @0 X(2)
		0xc3, 0xc3, 0xc3, 0xc3, 0xc3, 0xc3, 0xc3, 0xc3, // record 1: HDR-NAME @2 X(18)
		0xc3, 0xc3, 0xc3, 0xc3, 0xc3, 0xc3, 0xc3, 0xc3, //
		0xc3, 0xc3, //
		0xc8, 0xc4, // record 2: HDR-TYPE @0 X(2)
		0xc3, 0xc3, 0xc3, 0xc3, 0xc3, 0xc3, 0xc3, 0xc3, // record 2: HDR-NAME @2 X(18)
		0xc3, 0xc3, 0xc3, 0xc3, 0xc3, 0xc3, 0xc3, 0xc3, //
		0xc3, 0xc3, //
	}

	r, err := batched.NewReader(bytes.NewReader(in), batched.Encoding())
	if err != nil {
		t.Fatalf("NewReader: %v", err)
	}

	var records []batched.Record

	for {
		rec, err := r.Next()
		if errors.Is(err, io.EOF) {
			break
		}

		if err != nil {
			t.Fatalf("Next: %v", err)
		}

		records = append(records, rec)
	}

	if len(records) != 2 {
		t.Fatalf("the file holds 2 records and the reader produced %d", len(records))
	}

	record1, ok := records[0].(*batched.BatchHeader)
	if !ok {
		t.Fatalf("record 1 is BATCH-HEADER and came back as a %T, want a *batched.BatchHeader", records[0])
	}

	if record1.HdrType != "HD" {
		t.Errorf("HDR-TYPE: got %q, want %q", record1.HdrType, "HD")
	}

	record2, ok := records[1].(*batched.BatchHeader)
	if !ok {
		t.Fatalf("record 2 is BATCH-HEADER and came back as a %T, want a *batched.BatchHeader", records[1])
	}

	if record2.HdrType != "HD" {
		t.Errorf("HDR-TYPE: got %q, want %q", record2.HdrType, "HD")
	}

	var out bytes.Buffer

	w, err := batched.NewWriter(&out, batched.Encoding())
	if err != nil {
		t.Fatalf("NewWriter: %v", err)
	}

	for _, rec := range records {
		if err := w.Write(rec); err != nil {
			t.Fatalf("Write: %v", err)
		}
	}

	if err := w.Close(); err != nil {
		t.Fatalf("Close: %v", err)
	}

	if !bytes.Equal(out.Bytes(), in) {
		t.Errorf("the file does not write back the bytes it was read from\n got: % x\nwant: % x", out.Bytes(), in)
	}
}

// TestABatchHeaderThenTwoBatchDetails is a file of BATCH-HEADER and two
// BATCH-DETAIL records: the bytes below, the records they read back as, and
// the same bytes written back.
//
// The discriminators it walks: HDR-TYPE holding "\xc8\xc4" and DTL-TYPE
// holding "\xc4\xe3".
func TestABatchHeaderThenTwoBatchDetails(t *testing.T) {
	t.Parallel()

	in := []byte{
		0xc8, 0xc4, // record 1: HDR-TYPE @0 X(2)
		0xc3, 0xc3, 0xc3, 0xc3, 0xc3, 0xc3, 0xc3, 0xc3, // record 1: HDR-NAME @2 X(18)
		0xc3, 0xc3, 0xc3, 0xc3, 0xc3, 0xc3, 0xc3, 0xc3, //
		0xc3, 0xc3, //
		0xc1, 0xc1, 0xc1, 0xc1, 0xc1, 0xc1, 0xc1, 0xc1, // record 2: DTL-ACCOUNT @0 X(10)
		0xc1, 0xc1, //
		0xc4, 0xe3, // record 2: DTL-TYPE @10 X(2)
		0xd4, 0xd4, 0xd4, 0xd4, 0xd4, 0xd4, 0xd4, 0xd4, // record 2: DTL-MEMO @12 X(8)
		0xc1, 0xc1, 0xc1, 0xc1, 0xc1, 0xc1, 0xc1, 0xc1, // record 3: DTL-ACCOUNT @0 X(10)
		0xc1, 0xc1, //
		0xc4, 0xe3, // record 3: DTL-TYPE @10 X(2)
		0xd4, 0xd4, 0xd4, 0xd4, 0xd4, 0xd4, 0xd4, 0xd4, // record 3: DTL-MEMO @12 X(8)
	}

	r, err := batched.NewReader(bytes.NewReader(in), batched.Encoding())
	if err != nil {
		t.Fatalf("NewReader: %v", err)
	}

	var records []batched.Record

	for {
		rec, err := r.Next()
		if errors.Is(err, io.EOF) {
			break
		}

		if err != nil {
			t.Fatalf("Next: %v", err)
		}

		records = append(records, rec)
	}

	if len(records) != 3 {
		t.Fatalf("the file holds 3 records and the reader produced %d", len(records))
	}

	record1, ok := records[0].(*batched.BatchHeader)
	if !ok {
		t.Fatalf("record 1 is BATCH-HEADER and came back as a %T, want a *batched.BatchHeader", records[0])
	}

	if record1.HdrType != "HD" {
		t.Errorf("HDR-TYPE: got %q, want %q", record1.HdrType, "HD")
	}

	record2, ok := records[1].(*batched.BatchDetail)
	if !ok {
		t.Fatalf("record 2 is BATCH-DETAIL and came back as a %T, want a *batched.BatchDetail", records[1])
	}

	if record2.DtlType != "DT" {
		t.Errorf("DTL-TYPE: got %q, want %q", record2.DtlType, "DT")
	}

	record3, ok := records[2].(*batched.BatchDetail)
	if !ok {
		t.Fatalf("record 3 is BATCH-DETAIL and came back as a %T, want a *batched.BatchDetail", records[2])
	}

	if record3.DtlType != "DT" {
		t.Errorf("DTL-TYPE: got %q, want %q", record3.DtlType, "DT")
	}

	var out bytes.Buffer

	w, err := batched.NewWriter(&out, batched.Encoding())
	if err != nil {
		t.Fatalf("NewWriter: %v", err)
	}

	for _, rec := range records {
		if err := w.Write(rec); err != nil {
			t.Fatalf("Write: %v", err)
		}
	}

	if err := w.Close(); err != nil {
		t.Fatalf("Close: %v", err)
	}

	if !bytes.Equal(out.Bytes(), in) {
		t.Errorf("the file does not write back the bytes it was read from\n got: % x\nwant: % x", out.Bytes(), in)
	}
}
