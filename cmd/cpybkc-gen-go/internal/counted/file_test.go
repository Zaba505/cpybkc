// Code generated by cpybkc-gen-go. DO NOT EDIT.

// The file tier of this package's generated tests: one case per path through the
// automaton this layout describes, and between them every transition predicate it
// carries and every literal of a set-membership one.
//
// Each case carries a whole file as a literal — the framing as well as the records
// — reads it with the generated Reader, checks that the record types came back in
// the order the automaton admits them, writes them back with the generated Writer
// and checks that the bytes are the bytes it started from. The file is synthesized
// from the layout rather than read from a dataset, so it is what to hold against
// the file on your desk: the comment column names each run, the offset inside its
// record, and the picture.
//
// Nothing here is yours to edit — this directory is regenerated whole. Put your
// own tests in a package of your own that imports counted.
package counted_test

import (
	"bytes"
	"errors"
	"github.com/Zaba505/cpybkc/cmd/cpybkc-gen-go/internal/counted"
	"io"
	"testing"
)

// TestTwoHeaderRecords is a file of two HEADER-RECORD records: the bytes
// below, the records they read back as, and the same bytes written back.
//
// The discriminators it walks: TYPE-CODE holding "\xc8".
func TestTwoHeaderRecords(t *testing.T) {
	t.Parallel()

	in := []byte{
		0xc8,       // record 1: TYPE-CODE @0 X(1)
		0xf0, 0xf0, // record 1: DTL-COUNT @1 9(2)
		0xd5,       // record 1: SUM-FLAG @3 X(1)
		0xf0, 0xf5, // record 1: TOTAL-COUNT @4 9(2)
		0x15,       // the delimiter behind record 1
		0xc8,       // record 2: TYPE-CODE @0 X(1)
		0xf0, 0xf0, // record 2: DTL-COUNT @1 9(2)
		0xd5,       // record 2: SUM-FLAG @3 X(1)
		0xf0, 0xf5, // record 2: TOTAL-COUNT @4 9(2)
		0x15, // the delimiter behind record 2
	}

	r, err := counted.NewReader(bytes.NewReader(in), counted.Encoding())
	if err != nil {
		t.Fatalf("NewReader: %v", err)
	}

	var records []counted.Record

	for {
		rec, err := r.Next()
		if errors.Is(err, io.EOF) {
			break
		}

		if err != nil {
			t.Fatalf("Next: %v", err)
		}

		records = append(records, rec)
	}

	if len(records) != 2 {
		t.Fatalf("the file holds 2 records and the reader produced %d", len(records))
	}

	record1, ok := records[0].(*counted.HeaderRecord)
	if !ok {
		t.Fatalf("record 1 is HEADER-RECORD and came back as a %T, want a *counted.HeaderRecord", records[0])
	}

	if record1.TypeCode != "H" {
		t.Errorf("TYPE-CODE: got %q, want %q", record1.TypeCode, "H")
	}

	record2, ok := records[1].(*counted.HeaderRecord)
	if !ok {
		t.Fatalf("record 2 is HEADER-RECORD and came back as a %T, want a *counted.HeaderRecord", records[1])
	}

	if record2.TypeCode != "H" {
		t.Errorf("TYPE-CODE: got %q, want %q", record2.TypeCode, "H")
	}

	var out bytes.Buffer

	w, err := counted.NewWriter(&out, counted.Encoding())
	if err != nil {
		t.Fatalf("NewWriter: %v", err)
	}

	for _, rec := range records {
		if err := w.Write(rec); err != nil {
			t.Fatalf("Write: %v", err)
		}
	}

	if err := w.Close(); err != nil {
		t.Fatalf("Close: %v", err)
	}

	if !bytes.Equal(out.Bytes(), in) {
		t.Errorf("the file does not write back the bytes it was read from\n got: % x\nwant: % x", out.Bytes(), in)
	}
}

// TestAHeaderRecordThenTwoDetailRecords is a file of HEADER-RECORD and two
// DETAIL-RECORD records: the bytes below, the records they read back as, and
// the same bytes written back.
//
// The discriminators it walks: TYPE-CODE holding "\xc8" and TYPE-CODE holding
// "\xc4".
func TestAHeaderRecordThenTwoDetailRecords(t *testing.T) {
	t.Parallel()

	in := []byte{
		0xc8,       // record 1: TYPE-CODE @0 X(1)
		0xf0, 0xf2, // record 1: DTL-COUNT @1 9(2)
		0xd5,       // record 1: SUM-FLAG @3 X(1)
		0xf0, 0xf5, // record 1: TOTAL-COUNT @4 9(2)
		0x15,             // the delimiter behind record 1
		0xc4,             // record 2: TYPE-CODE @0 X(1)
		0x00, 0x00, 0x2d, // record 2: AMOUNT @1 S9(3)V9(2) PACKED-DECIMAL
		0x15,             // the delimiter behind record 2
		0xc4,             // record 3: TYPE-CODE @0 X(1)
		0x00, 0x00, 0x2d, // record 3: AMOUNT @1 S9(3)V9(2) PACKED-DECIMAL
		0x15, // the delimiter behind record 3
	}

	r, err := counted.NewReader(bytes.NewReader(in), counted.Encoding())
	if err != nil {
		t.Fatalf("NewReader: %v", err)
	}

	var records []counted.Record

	for {
		rec, err := r.Next()
		if errors.Is(err, io.EOF) {
			break
		}

		if err != nil {
			t.Fatalf("Next: %v", err)
		}

		records = append(records, rec)
	}

	if len(records) != 3 {
		t.Fatalf("the file holds 3 records and the reader produced %d", len(records))
	}

	record1, ok := records[0].(*counted.HeaderRecord)
	if !ok {
		t.Fatalf("record 1 is HEADER-RECORD and came back as a %T, want a *counted.HeaderRecord", records[0])
	}

	if record1.TypeCode != "H" {
		t.Errorf("TYPE-CODE: got %q, want %q", record1.TypeCode, "H")
	}

	record2, ok := records[1].(*counted.DetailRecord)
	if !ok {
		t.Fatalf("record 2 is DETAIL-RECORD and came back as a %T, want a *counted.DetailRecord", records[1])
	}

	if record2.TypeCode != "D" {
		t.Errorf("TYPE-CODE: got %q, want %q", record2.TypeCode, "D")
	}

	record3, ok := records[2].(*counted.DetailRecord)
	if !ok {
		t.Fatalf("record 3 is DETAIL-RECORD and came back as a %T, want a *counted.DetailRecord", records[2])
	}

	if record3.TypeCode != "D" {
		t.Errorf("TYPE-CODE: got %q, want %q", record3.TypeCode, "D")
	}

	var out bytes.Buffer

	w, err := counted.NewWriter(&out, counted.Encoding())
	if err != nil {
		t.Fatalf("NewWriter: %v", err)
	}

	for _, rec := range records {
		if err := w.Write(rec); err != nil {
			t.Fatalf("Write: %v", err)
		}
	}

	if err := w.Close(); err != nil {
		t.Fatalf("Close: %v", err)
	}

	if !bytes.Equal(out.Bytes(), in) {
		t.Errorf("the file does not write back the bytes it was read from\n got: % x\nwant: % x", out.Bytes(), in)
	}
}

// TestAHeaderRecordThenASummaryRecord is a file of HEADER-RECORD and
// SUMMARY-RECORD: the bytes below, the records they read back as, and the same
// bytes written back.
//
// The discriminators it walks: TYPE-CODE holding "\xc8" and TYPE-CODE holding
// "\xe2".
func TestAHeaderRecordThenASummaryRecord(t *testing.T) {
	t.Parallel()

	in := []byte{
		0xc8,       // record 1: TYPE-CODE @0 X(1)
		0xf0, 0xf0, // record 1: DTL-COUNT @1 9(2)
		0xe8,       // record 1: SUM-FLAG @3 X(1)
		0xf0, 0xf1, // record 1: TOTAL-COUNT @4 9(2)
		0x15,             // the delimiter behind record 1
		0xe2,             // record 2: TYPE-CODE @0 X(1)
		0xc2, 0xc2, 0xc2, // record 2: LINE(1).LINE-TEXT @1 X(3)
		0xc5, 0xc5, // record 2: NOTE(1).NOTE-TEXT @4 X(2)
		0x15, // the delimiter behind record 2
	}

	r, err := counted.NewReader(bytes.NewReader(in), counted.Encoding())
	if err != nil {
		t.Fatalf("NewReader: %v", err)
	}

	var records []counted.Record

	for {
		rec, err := r.Next()
		if errors.Is(err, io.EOF) {
			break
		}

		if err != nil {
			t.Fatalf("Next: %v", err)
		}

		records = append(records, rec)
	}

	if len(records) != 2 {
		t.Fatalf("the file holds 2 records and the reader produced %d", len(records))
	}

	record1, ok := records[0].(*counted.HeaderRecord)
	if !ok {
		t.Fatalf("record 1 is HEADER-RECORD and came back as a %T, want a *counted.HeaderRecord", records[0])
	}

	if record1.TypeCode != "H" {
		t.Errorf("TYPE-CODE: got %q, want %q", record1.TypeCode, "H")
	}

	record2, ok := records[1].(*counted.SummaryRecord)
	if !ok {
		t.Fatalf("record 2 is SUMMARY-RECORD and came back as a %T, want a *counted.SummaryRecord", records[1])
	}

	if record2.TypeCode != "S" {
		t.Errorf("TYPE-CODE: got %q, want %q", record2.TypeCode, "S")
	}

	var out bytes.Buffer

	w, err := counted.NewWriter(&out, counted.Encoding())
	if err != nil {
		t.Fatalf("NewWriter: %v", err)
	}

	for _, rec := range records {
		if err := w.Write(rec); err != nil {
			t.Fatalf("Write: %v", err)
		}
	}

	if err := w.Close(); err != nil {
		t.Fatalf("Close: %v", err)
	}

	if !bytes.Equal(out.Bytes(), in) {
		t.Errorf("the file does not write back the bytes it was read from\n got: % x\nwant: % x", out.Bytes(), in)
	}
}
