// Code generated by cpybkc-gen-go. DO NOT EDIT.

// The record tier of this package's generated tests: one case per record the
// layout describes, one per arm of an alternation inside one, and one more per
// record carrying a table that may be absent, holding that record at its
// shortest.
//
// Each case carries the bytes it reads as a literal, decodes them, checks every
// field against the value the literal was laid out with, and writes the record
// back out byte for byte. The bytes are synthesized from the layout rather than
// read from a file, so they are what to hold against the file on your desk: the
// comment column names the item, the offset summed from the widths ahead of it,
// and the picture.
//
// Nothing here is yours to edit — this directory is regenerated whole. Put your
// own tests in a package of your own that imports member.
package member_test

import (
	"bytes"
	"github.com/Zaba505/cobol-go/codec"
	"github.com/Zaba505/cpybkc/example/member/member"
	"testing"
)

// TestMemberRecordReadsBackTheBytesItWasReadFrom is MEMBER-RECORD: the bytes
// below, every field they decode into, and the same bytes written back.
func TestMemberRecordReadsBackTheBytesItWasReadFrom(t *testing.T) {
	t.Parallel()

	in := []byte{
		0xc1, 0xc1, 0xc1, 0xc1, 0xc1, 0xc1, 0xc1, 0xc1, // MBR-MEMBER-ID @0 X(10)
		0xc1, 0xc1, //
		0xd2, 0xd2, 0xd2, 0xd2, 0xd2, 0xd2, 0xd2, 0xd2, // MBR-NAME @10 X(30)
		0xd2, 0xd2, 0xd2, 0xd2, 0xd2, 0xd2, 0xd2, 0xd2, //
		0xd2, 0xd2, 0xd2, 0xd2, 0xd2, 0xd2, 0xd2, 0xd2, //
		0xd2, 0xd2, 0xd2, 0xd2, 0xd2, 0xd2, //
		0xf1,                                           // MBR-ADDRESS-COUNT @40 9(1)
		0xd7, 0xd7, 0xd7, 0xd7, 0xd7, 0xd7, 0xd7, 0xd7, // MBR-ADDRESS(1).ADR-HOME.AHM-STREET @41 X(30)
		0xd7, 0xd7, 0xd7, 0xd7, 0xd7, 0xd7, 0xd7, 0xd7, //
		0xd7, 0xd7, 0xd7, 0xd7, 0xd7, 0xd7, 0xd7, 0xd7, //
		0xd7, 0xd7, 0xd7, 0xd7, 0xd7, 0xd7, //
		0xe3, 0xe3, 0xe3, 0xe3, 0xe3, 0xe3, 0xe3, 0xe3, // MBR-ADDRESS(1).ADR-HOME.AHM-CITY @71 X(20)
		0xe3, 0xe3, 0xe3, 0xe3, 0xe3, 0xe3, 0xe3, 0xe3, //
		0xe3, 0xe3, 0xe3, 0xe3, //
		0xd5, 0xd5, // MBR-ADDRESS(1).ADR-HOME.AHM-REGION @91 X(2)
		0xd7, 0xd7, 0xd7, 0xd7, 0xd7, 0xd7, 0xd7, 0xd7, // MBR-ADDRESS(1).ADR-HOME.AHM-POSTAL @93 X(9)
		0xd7, //
		0xe8, // MBR-STATUS @102 X(1)
	}

	r, err := codec.NewReader(bytes.NewReader(in), member.Encoding())
	if err != nil {
		t.Fatalf("codec.NewReader: %v", err)
	}

	var record member.MemberRecord

	if err := record.UnmarshalCOBOL(r); err != nil {
		t.Fatalf("UnmarshalCOBOL: %v", err)
	}

	if record.MbrMemberId != "AAAAAAAAAA" {
		t.Errorf("MBR-MEMBER-ID: got %q, want %q", record.MbrMemberId, "AAAAAAAAAA")
	}

	if record.MbrName != "KKKKKKKKKKKKKKKKKKKKKKKKKKKKKK" {
		t.Errorf("MBR-NAME: got %q, want %q", record.MbrName, "KKKKKKKKKKKKKKKKKKKKKKKKKKKKKK")
	}

	if record.MbrAddressCount != 1 {
		t.Errorf("MBR-ADDRESS-COUNT: got %d, want %d", record.MbrAddressCount, 1)
	}

	if len(record.MbrAddress) != 1 {
		t.Fatalf("MBR-ADDRESS: got %d occurrences, want %d", len(record.MbrAddress), 1)
	}

	if record.MbrAddress[0].AdrHome == nil {
		t.Fatalf("MBR-ADDRESS(1).ADR-HOME: the record holds no arm, and the schedule assigns this one to this occurrence")
	}

	if record.MbrAddress[0].AdrWork != nil {
		t.Errorf("MBR-ADDRESS(1).ADR-WORK: the record holds this arm, and the schedule assigns another to this occurrence")
	}

	if record.MbrAddress[0].AdrMail != nil {
		t.Errorf("MBR-ADDRESS(1).ADR-MAIL: the record holds this arm, and the schedule assigns another to this occurrence")
	}

	if record.MbrAddress[0].AdrHome.AhmStreet != "PPPPPPPPPPPPPPPPPPPPPPPPPPPPPP" {
		t.Errorf("MBR-ADDRESS(1).ADR-HOME.AHM-STREET: got %q, want %q", record.MbrAddress[0].AdrHome.AhmStreet, "PPPPPPPPPPPPPPPPPPPPPPPPPPPPPP")
	}

	if record.MbrAddress[0].AdrHome.AhmCity != "TTTTTTTTTTTTTTTTTTTT" {
		t.Errorf("MBR-ADDRESS(1).ADR-HOME.AHM-CITY: got %q, want %q", record.MbrAddress[0].AdrHome.AhmCity, "TTTTTTTTTTTTTTTTTTTT")
	}

	if record.MbrAddress[0].AdrHome.AhmRegion != "NN" {
		t.Errorf("MBR-ADDRESS(1).ADR-HOME.AHM-REGION: got %q, want %q", record.MbrAddress[0].AdrHome.AhmRegion, "NN")
	}

	if record.MbrAddress[0].AdrHome.AhmPostal != "PPPPPPPPP" {
		t.Errorf("MBR-ADDRESS(1).ADR-HOME.AHM-POSTAL: got %q, want %q", record.MbrAddress[0].AdrHome.AhmPostal, "PPPPPPPPP")
	}

	if record.MbrStatus != "Y" {
		t.Errorf("MBR-STATUS: got %q, want %q", record.MbrStatus, "Y")
	}

	var out bytes.Buffer

	w, err := codec.NewWriter(&out, member.Encoding())
	if err != nil {
		t.Fatalf("codec.NewWriter: %v", err)
	}

	if err := record.MarshalCOBOL(w); err != nil {
		t.Fatalf("MarshalCOBOL: %v", err)
	}

	if !bytes.Equal(out.Bytes(), in) {
		t.Errorf("the record does not write back the bytes it was read from\n got: % x\nwant: % x", out.Bytes(), in)
	}
}
